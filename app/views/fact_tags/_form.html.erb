<%= form_for(fact_tag) do |f| %>
  <% if fact_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fact_tag.errors.count, "error") %> prohibited this fact_tag from being saved:</h2>

      <ul>
      <% fact_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fact_id %>
    <%= f.text_field :fact_id %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
