<%= form_for(fact) do |f| %>
  <% if fact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fact.errors.count, "error") %> prohibited this fact from being saved:</h2>

      <ul>
      <% fact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body, :style => "height: 500px; width: 400px;"%>
  </div>

  <div class="field">
    <%= f.label :fact_id %>
    <%= f.number_field :fact_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
