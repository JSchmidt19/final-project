
<%= form_for(@pot_image) do |f| %>
  <% if @pot_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pot_image.errors.count, "error") %> prohibited this pot_image from being saved:</h2>

      <ul>
      <% @pot_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pot_id %>
    <%= f.text_field :pot_id %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field :image_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

